<% provide(:title, 'All Users') %>
<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <!-- this causes rails to look for a _user.html.erb partial-->
      <%= render user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
